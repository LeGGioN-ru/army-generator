# Army Generator

## 🛠 Используемые паттерны и подходы

В проекте были применены следующие архитектурные решения:

1.  **Zenject (DI)**
    Использовался, дабы упростить инициализацию зависимостей. В будущем может использоваться для подмены реализаций под интерфейсами.

2.  **Object Pool**
    Реализован, чтобы постоянно не инстанциировать и не удалять объекты (`Instantiate`/`Destroy`). Тем самым повышаем производительность и снижаем нагрузку на GC.

3.  **State Machine (FSM)**
    Используется для управления поведением юнитов, т.к. состояний достаточно много (Idle, Search, Attack). Дабы код в кашу не превращался — логика разбита на стейты и транзишены. Транзишены реализованы структурами (`struct`), дабы попроще для памяти было их обрабатывать.

4.  **MVP (Model-View-Presenter)**
    Для солдата MVP. Не чистый — имею в виду не чистый C#, а с примесью монобехов, дабы не переусложнять работу/дебагинг/инициализацию/сереализацию в Юнити. Ну и MVP позволяет удобно разграничить данные и не перемешивать логику визуала с логикой модели и логикой взаимодействия. Плюс позволит в будущем грузить модели из другого источника (локальный json, бэкенд).

5.  **Factory**
    Используется, т.к. обычного `Instantiate` недостаточно. Фабрика служит местом, где мы инициализируем, создаем и конфигурируем (рандомим статы) наших солдат перед выдачей.

6.  **Signal Bus**
    Используется встроенная в Zenject сигнальная шина. Удобно прокидывать глобальные события (например, конец игры) и отслеживать логику поведения приложения, подписываясь на события в нужных системах.

7.  **Система Статов**
    Реализация статов и их отображения разделена:
    * Есть модели, отвечающие за сами статы (можно удобно докидывать/убирать).
    * Есть модели для вьюшек (цвет, форма, размер).
    * Сами вьюшки принимают `StatView`, в которых уже есть `Stats`.
    * Это позволит удобно переиспользовать статы в будущем + не сильно связываем модели для вью и модели для статов.

8.  **Интерфейс ISoldier**
    Интерфейс `ISoldier` сделан, дабы не прокидывать `SoldierPresenter` целиком в стейт-машину. По контексту задачи этого достаточно, но в будущем можно разделить интерфейс ещё сильнее (Interface Segregation).

9.  **Доп фича**
   Доп. фича игры — ставки. Гемблинг нынче востребован.

---

### 🚀 Запуск проекта

1. Открыть сцену `Boot` (или `MainScene`).
2. Запустить Play Mode.
